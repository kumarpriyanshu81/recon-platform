# recon-platform — Python dependencies
#
# The core framework has zero mandatory Python dependencies (stdlib only).
# External tool binaries (subfinder, httpx-toolkit) are expected on PATH.
#
# Development / testing
pytest>=8.0
